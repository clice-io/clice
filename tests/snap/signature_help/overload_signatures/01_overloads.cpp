/// # Function overloads
///
/// - status: supported
///
/// Every overload of the callee, each with its parameter list and return type

void foo();
void foo(int x);
void foo(int x, int y);

int main() {
    foo(§(pos));
}
