name: PR Smoke

on:
  pull_request:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "hello"
          echo "ref=${{ github.ref }}"
          echo "sha=${{ github.sha }}"