/// Primary interface of module clice.syntax. Re-exports every partition.
/// Keep sorted.
export module clice.syntax;

export import :completion;
export import :dependency_graph;
export import :include_resolver;
export import :lexer;
export import :preamble_synthesis;
export import :scan;
export import :token;
