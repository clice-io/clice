/// Primary interface of module clice.feature. Re-exports every partition.
/// Keep sorted.
export module clice.feature;

export import :document_link;
export import :feature;
export import :inactive_regions;
