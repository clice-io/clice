/// Primary interface of module clice.index. Re-exports every partition.
/// Keep sorted.
export module clice.index;

export import :include_graph;
export import :merged_index;
export import :path_pool;
export import :preamble_state;
export import :project_index;
export import :serialization;
export import :shared;
export import :tu_index;
export import :usr;
