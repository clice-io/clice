/// Primary interface of module clice.command. Re-exports every partition.
/// Keep sorted.
export module clice.command;

export import :argument_parser;
export import :command;
export import :search_config;
export import :toolchain;
